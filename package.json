{
  "name": "mern-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "client": "npm run dev --prefix feUser",
    "server": "npm run dev --prefix be",
    "clientAdmin": "npm run dev --prefix feAdmin",
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run clientAdmin\""
  },
  "dependencies": {
    "concurrently": "^8.0.1"  
  }
}
